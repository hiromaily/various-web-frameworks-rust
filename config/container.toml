app_name = "api-server"

[server]
host = "0.0.0.0"
port = 8080

[jwt]
kind = "jsonwebtoken" # jwt-simple, jsonwebtoken, none
duration_min = 30

[logger]
service = "api-server"
level = "debug"        # debug, info, warn, error

[db]
enabled = true
host = "postgresql:5432"
dbname = "example"
user = "admin"
password = "admin"
